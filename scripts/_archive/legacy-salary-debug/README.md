# Legacy Salary Debug Scripts (Archived)

These scripts are archived because they are not part of active automation:
- not referenced by `package.json` scripts
- not used by GitHub workflows
- superseded by newer repair/audit flows

If you need one for incident forensics, run it directly from this folder and migrate any useful logic into a maintained script before reuse.
